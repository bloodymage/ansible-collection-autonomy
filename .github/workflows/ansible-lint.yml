---
name: Ansible Lint

on:
  push:
    branches:
      ['main', 'develop']
  pull_request:
    branches:
      ['main', 'develop']

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Run ansilbe-lint
        uses: ansible/ansible-lint@main
