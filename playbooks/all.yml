# ==================================================================================================
#
# This play has been split into to stages.  While there may be a better way, the second stage is to
# reset unreachable hosts so that certificates can be generated for them locally.
#
# ==================================================================================================
- import_playbook: all_stage1.yml
- import_playbook: all_stage2.yml
