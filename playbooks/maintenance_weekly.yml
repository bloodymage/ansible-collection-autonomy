---
- hosts: all
  roles:
    - role: bloodymage.autonomy.autoconfig
      tags:
        - always
      when:
        - ansible_date_time.weekday == "Monday"

- name: "Update ACME Certificates."
  hosts: web_servers:mail_servers:mosquitto_servers:&public_zone
  roles:
    - role: bloodymage.autonomy.acmeca
      vars:
        __acmeca_tasks: "account"
    - role: bloodymage.autonomy.acmeca
      vars:
        __acmeca_tasks: "certs"

- name: "Configure Databases."
  hosts: mariadb_servers:postgresql_servers:mysql_servers
  roles:
    - role: bloodymage.autonomy.maintenance
      vars:
        __maintenance_tasks: "database"
      tags:
        - databases
        - database
