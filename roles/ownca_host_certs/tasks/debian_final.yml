---
# ==================================================================================================
#
# Tasks: Copy Host Certificate back to Server.
#  
#
# ==================================================================================================
- name: "Copy Host Certificate back to server."
  ansible.builtin.copy:
    src: "{{ ownca_hosts_directory }}/certs/{{ item }}.crt"
    dest: "/usr/local/share/ca-certificates/{{ item }}.crt"
    owner: "root"
    group: "root"
    mode: "0644"
  become: yes
  loop:
    - "{{ inventory_hostname }}"
    - "{{ inventory_hostname }}-chain"
  notify: restart all
  tags:
    - ownca_host_certs
    - ownca-host-certs
    - ownca_host_certs_debug
    - ownca-host-certs-debug
    - ownca-all

- name: Copy domain controllers' certificate to server.
  ansible.builtin.copy:
    src: "{{ ownca_hosts_directory }}/certs/{{ item }}.crt"
    dest: "/usr/local/share/ca-certificates/{{ item }}.crt"
    owner: root
    group: root
    mode: "0644"
  become: yes
  loop: "{{ groups['samba_domain_controllers'] | default([]) }}"
  when:
    - rebeldream_realm_identity_management_system is truthy
  notify: restart all
  tags:
    - ownca_host_certs
    - ownca-host-certs
    - ownca_host_certs_debug
    - ownca-host-certs-debug
    - ownca-all

- name: Copy domain controllers' certificate chain to server.
  ansible.builtin.copy:
    src: "{{ ownca_hosts_directory }}/certs/{{ item }}-chain.crt"
    dest: "/usr/local/share/ca-certificates/{{ item }}-chain.crt"
    owner: root
    group: root
    mode: "0644"
  become: yes
  loop: "{{ groups['samba_domain_controllers'] | default([]) }}"
  when:
    - rebeldream_realm_identity_management_system is truthy
  notify: restart all
  tags:
    - ownca_host_certs
    - ownca-host-certs
    - ownca_host_certs_debug
    - ownca-host-certs-debug
    - ownca-all

- name: Copy CA Certificates to server.
  ansible.builtin.copy:
    src: "{{ ownca_ca_directory }}/certs/{{ rebeldream_org_name }}-{{ item.name }}-ca.crt"
    dest: "/usr/local/share/ca-certificates/{{ rebeldream_org_name }}-{{ item.name }}-ca.crt"
    owner: root
    group: root
    mode: "0644"
  loop: "{{ ownca_certificate_authorities }}"
  become: yes
  notify: restart all
  tags:
    - ownca_host_certs
    - ownca-host-certs
    - ownca_host_certs_debug
    - ownca-host-certs-debug
    - ownca-all

- name: Copy CA Chains Certificates to server.
  ansible.builtin.copy:
    src: "{{ ownca_ca_directory }}/certs/{{ rebeldream_org_name }}-{{ item.name }}-ca-chain.crt"
    dest: "/usr/local/share/ca-certificates/{{ rebeldream_org_name }}-{{ item.name }}-ca-chain.crt"
    owner: root
    group: root
    mode: "0644"
  loop: "{{ ownca_certificate_authorities }}"
  become: yes
  notify: restart all
  when:
    - item.name != "root"
  tags:
    - ownca_host_certs
    - ownca-host-certs
    - ownca_host_certs_debug
    - ownca-host-certs-debug
    - ownca-all

- name: Copy CRLs to server.
  ansible.builtin.copy:
    src: "{{ ownca_ca_directory }}/crl/{{ rebeldream_org_name }}-{{ item.name }}.crl"
    dest: "{{ ownca_etc }}/crls/{{ rebeldream_org_name }}-{{ item.name }}.crl"
    owner: root
    group: root
    mode: "0644"
  loop: "{{ ownca_certificate_authorities }}"
  become: yes
  tags:
    - ownca_crl
    - ownca-crl
    - ownca_crl_debug
    - ownca-crl-debug

- name: "Ensure we have a PKCS12 password for the Host"
  ansible.builtin.set_fact:
    ownca_host_certs_pkcs12_password: "{{ lookup('community.general.passwordstore', __password_lookup) }}"
  vars:
    __password_store_id: "{{ rebeldream_domain }}/hosts/{{ rebeldream_hostname }}/ownca_host_cert_pkcs12_password"
    __password_length: "length={{ rebeldream_password_length }}"
    __password_overwrite: "overwrite={{ rebeldream_overwrite_password }} backup=yes"
    __password_create: "create=true"
    __password_symbols: "nosymbols=true"
    __password_lookup: "{{ __password_store_id }} {{ __password_create }} {{ __password_length }} {{ __password_symbols }} {{ __password_overwrite }}"
  delegate_to: localhost
  when:
    - ownca_host_certs_pkcs12_password == "password"
  tags:
    - ownca_host_certs
    - ownca-host-certs
    - ownca_host_certs_debug
    - ownca-host-certs-debug

- name: Generate PKCS#12 file
  community.crypto.openssl_pkcs12:
    action: export
    backup: yes
    path:  "{{ ownca_etc }}/certs/{{ rebeldream_fqdn }}.pkcs12"
    friendly_name: "{{ rebeldream_fqdn }}"
    passphrase: "{{ ownca_host_certs_pkcs12_password }}"
    privatekey_path: "{{ ownca_etc }}/private/{{ rebeldream_fqdn }}.key"
    certificate_path: "{{ ownca_etc }}/certs/{{ rebeldream_fqdn }}-chain.pem"
    other_certificates:
      - "{{ ownca_etc }}/certs/{{ rebeldream_org_name }}-{{ rebeldream_org_unit_name }}-component-ca.pem"
      - "{{ ownca_etc }}/certs/{{ rebeldream_org_name }}-{{ rebeldream_org_unit_name }}-ca.pem"
      - "{{ ownca_etc }}/certs/{{ rebeldream_org_name }}-root-ca.pem"
    state: present
    iter_size: 0
    maciter_size: 0
    owner: root
    group: root
    mode: '0600'
    force: yes
  become: yes
  tags:
    - ownca_host_certs
    - ownca-host-certs
    - ownca_host_certs_debug
    - ownca-host-certs-debug

# - name: Fetch PKCS#12 files
#   ansible.builtin.fetch:
#     src: "{{ ownca_etc }}/csr/{{ inventory_hostname }}.csr"
#     dest: "{{ ownca_hosts_directory }}/csr/"
#     flat: yes
#   become: yes
#   tags:
#     - ownca_host_certs
#     - ownca-host-certs
#     - ownca_host_certs_debug
#     - ownca-host-certs-debug
#     - ownca-all
