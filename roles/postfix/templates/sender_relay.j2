# {{ ansible_managed }}

{% for user in postfix_sasl_auth_users_list -%}
  {{ user.origin.ljust(20) }} {{ postfix_relay_host }}
{% endfor %}
{{ postfix_sasl_auth_user.ljust(20) }} {{ postfix_relay_host }}
