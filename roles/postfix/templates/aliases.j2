# {{ ansible_managed }}

{% for user in users %}
  {% if user.email_alias is defined %}
    {{ user.username }}: {{ user.email_alias }}
  {% endif %}
{% endfor %}
# Need a way to determine if these users exist on the host
webmaster: webmaster@{{ ansible_domain }}
www: webmaster@{{ ansible_domain }}
www-data: webmaster@{{ ansible_domain }}
postmaster: postmaster@{{ ansible_domain }}
mailer-daemon: postmaster@{{ ansible_domain }}
abuse: abuse@{{ ansible_domain }}
nobody: webmaster@{{ ansible_domain }}
hostmaster: webmaster@{{ ansible_domain }}
# usenet: root
# news: root
# ftp: root
# noc: root
# security: root
# clamav: root
