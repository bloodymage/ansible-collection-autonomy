---
# tasks file for unattended_upgrades
# ======================================================================
#
# Tasks:
#
#
# ======================================================================
- name: "Include OS-specific variables."
  ansible.builtin.include_vars: "{{ lookup('first_found', params) }}"
  vars:
    params:
      files: "{{ __os_variables_files }}"
      paths:
        - "vars"
  become: false

- name: "Install and Configure Unattended Upgrades."
  ansible.builtin.include_tasks: "install.yml"
  when:
    - autonomy_unattendedupgrades_enabled

- name: "Remove Unattended Upgrades."
  ansible.builtin.include_tasks: "remove_{{ ansible_os_family | lower }}.yml"
  when:
    - autonomy_unattendedupgrades_enabled is falsy
