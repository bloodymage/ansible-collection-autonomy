---
# ==================================================================================================
#
# vmail
#
# ==================================================================================================
autonomy_common_users_vmail:
  username: vmail
  groups: []
  id_number: "{{ autonomy_common_users_vmail_id_number }}"
  unix_home: "/var/mail/vmail"
  ssh_access_cert: []
  ssh_cert:
    - None

autonomy_common_users_vmail_group:
  name: vmail
  gid: "{{ autonomy_common_users_vmail_id_number }}"
