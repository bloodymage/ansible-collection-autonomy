---
# ==================================================================================================
#
# User 1
#
# ==================================================================================================
autonomy_common_users_user1_username: "pi"
autonomy_common_users_user1_name: "{{ autonomy_common_users_user1_username | capitalize }}"
autonomy_common_users_user1_groups:
  - "users"
autonomy_common_users_user1_id_number: 1000
autonomy_common_users_user1_password: "password"
autonomy_common_users_user1_ssh_key_type: "{{ autonomy_common_users_ssh_key_type }}"
autonomy_common_users_user1_ssh_key_password: "password"
autonomy_common_users_user1_ssh_access_cert: []
autonomy_common_users_user1_ssh_principals: []
autonomy_common_users_user1_home_dir: "{{ autonomy_common_home_dir }}/{{ autonomy_common_users_user1_username }}"
autonomy_common_users_user1_gpg_keys: []

autonomy_common_users_user1:
  username: "{{ autonomy_common_users_user1_username }}"
  name: "{{ autonomy_common_users_user1_name }}"
  groups: "{{ autonomy_common_users_user1_groups }}"
  id_number: "{{ autonomy_common_users_user1_id_number }}"
  unix_home: "{{ autonomy_common_users_user1_home_dir }}"
  password: "{{ autonomy_common_users_user1_password }}"
  ssh_key_type: "{{ autonomy_common_users_user1_ssh_key_type }}"
  ssh_key_password: "{{ autonomy_common_users_user1_ssh_key_password }}"
  ssh_access_cert: "{{ autonomy_common_users_user1_ssh_access_cert }}"
  ssh_principals: "{{ autonomy_common_users_user1_ssh_principals }}"
  gpg_keys: "{{ autonomy_common_users_user1_gpg_keys }}"
  git_repos: "{{ autonomy_common_users_gitrepos }}"
