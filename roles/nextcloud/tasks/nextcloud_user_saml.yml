---
- name: "Set nextcloud user saml configuration."
  ansible.builtin.command:
    cmd: >-
      php occ config:app:set
      --value={{ item.saml_value }}
      user_saml
      {{ item.saml_attribute }}
    chdir: "/srv/www/{{ inventory_hostname }}"
  loop: "{{ nextcloud_saml_config }}"
  become: yes
  become_user: www-data

- name: "Get current identity provider certificate."
  community.crypto.x509_certificate_info:
    path: "{{ ownca_directory }}/hosts/certs/{{ nextcloud_identity_provider }}.crt"
  become: no
  register: __idp_certificate
  delegate_to: localhost
  when:
    - nextcloud_identity_provider is truthy

- name: "Get current nextcloud host private key."
  community.crypto.openssl_privatekey_info:
    path: "/etc/letsencrypt/archive/{{ inventory_hostname }}/privkey.pem"
    return_private_key_data: yes
  become: yes
  no_log: yes
  register: __sp_private_key

- name: "Get current nextcloud host certificate."
  community.crypto.x509_certificate_info:
    path: "/etc/letsencrypt/archive/{{ inventory_hostname }}/cert.pem"
  become: yes
  register: __sp_certificate

- name: "Update nextcloud configuration certificate."
  ansible.builtin.command:
    cmd: >-
      php occ config:app:set
      --value="{{ __idp_certificate.public_key }}"
      --update-only
      user_saml
      idp-x509cert
    chdir: "/srv/www/{{ inventory_hostname }}"
  become: yes
  become_user: www-data

- name: "Print private key."
  ansible.builtin.debug:
    msg:
      - "{{ __sp_private_key }}"
    
# - name: "Update nextcloud configuration public key."
#   ansible.builtin.command:
#     cmd: >-
#       php occ config:app:set
#       --value="{{ __sp_private_key.private_data }}"
#       --update-only
#       user_saml
#       sp-privateKey
#     chdir: "/srv/www/{{ inventory_hostname }}"
#   become: yes
#   become_user: www-data
#   no_log: yes

- name: "Update nextcloud configuration certificate."
  ansible.builtin.command:
    cmd: >-
      php occ config:app:set
      --value="{{ __sp_certificate.public_key }}"
      --update-only
      user_saml
      sp-x509cert
    chdir: "/srv/www/{{ inventory_hostname }}"
  become: yes
  become_user: www-data
