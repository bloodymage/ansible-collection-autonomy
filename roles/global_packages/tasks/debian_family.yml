---
# ==================================================================================================
#
# Task "Install Common Apps"
#
# Installs a set of common apps for all systems on the network
#
# ==================================================================================================
- name: "{{ ansible_os_family }}: Install Common Apps"
  apt:
    name: "{{ packages }}"
    state: present
  become: yes
  tags:
    - install-packages

# ==================================================================================================
#
# Task:
#
#
#
# ==================================================================================================
#- name: Install Ubuntu Apps
#  pip:
#    name: "{{ pip_packagkes }}"
#    executable: '/usr/bin/pip3'
#  when: ansible_distribution == 'Ubuntu'

